# CVF-01 开发规划

> 文档状态：正式开发路线基线  
> 编制日期：2026-07-24  
> 当前版本：`0.2.1`
> 当前分支：`main`  
> 当前阶段：Phase 3D 已完成，准备进行 Phase 3 总验收与 v0.3.0 发布
> 核心目标：构建一套基于 Binance USDⓈ-M 与 OKX v5 公共行情的短线永续合约研究、实时信号、模拟交易与历史验证系统。

---

## 1. 项目最终目标

CVF-01 的目标不是简单输出 `BUY` 或 `SELL`，而是建立一条可验证、可回放、可审计的完整研究链路：

```text
公共行情采集
→ 标准化与本地订单簿
→ 实时特征
→ 跨交易所对齐
→ 策略评分
→ 信号状态机
→ 实时通知
→ 模拟成交与风险控制
→ 历史回放与回测
→ 小额实盘前验证
```

项目首要原则：

1. 数据正确性优先于策略复杂度；
2. 实时和回放必须复用同一套核心逻辑；
3. 所有特征只能使用决策时点之前的数据；
4. 所有信号必须具备来源、原因、版本和有效期；
5. 在没有完成模拟交易与历史验证前，不连接真实账户；
6. 不允许因追求信号数量而降低数据健康标准；
7. 不把研究参数描述成已被证明有效的盈利参数。

---

## 2. 当前仓库审查结论

### 2.1 已具备的能力

Phase 2 已建立可信的公共行情底座，包括：

- Binance USDⓈ-M 公共 WebSocket 与 REST 行情采集；
- OKX v5 公共 WebSocket 与 REST 行情采集；
- BTC、ETH 永续合约统一交易对；
- 逐笔成交、最优报价、订单簿、持仓量、资金费率、标记价格、指数价格和公开强平样本；
- 基于 `Decimal` 的数量和价格规范化；
- Binance 快照与增量深度本地订单簿；
- OKX 序列与校验和本地订单簿；
- 断线重连、心跳、指数退避与抖动；
- 去重、数据延迟、时钟偏移和健康监控；
- 有界异步队列与原子 Parquet 写入；
- 原始载荷、UUID 和 `raw://record_id` 数据血缘；
- 默认命令完全离线，仅 `cvf collect` 显式访问公网；
- 严格 mypy、Ruff、pytest、wheel 独立安装和运行验证。

Phase 2 的工程质量足以支持 Phase 3，不需要重写现有连接器或订单簿。

### 2.2 进入 Phase 3 前必须处理的衔接点

#### P0：建立规范化事件分发管线

当前 `MarketDataCollector` 将规范化事件交给 `_record_event`，该回调主要负责计数。Phase 3 需要将事件交给多个独立消费者：

- 特征状态存储；
- 特征引擎；
- 特征持久化；
- 实时监控；
- 后续信号引擎。

不得直接把所有逻辑堆进连接器或 `collector.py`。

建议增加：

```text
NormalizedEventBus
├─ FeaturePipeline
├─ FeatureStorageSink
├─ MetricsSink
└─ FutureSignalPipeline
```

事件分发必须：

- 保持单个交易所、交易对和频道内的顺序；
- 使用有界队列；
- 可观测队列深度和处理延迟；
- 消费者失败时不能静默丢失；
- 支持优雅关闭和最终刷新；
- 支持实时模式与回放模式共用。

#### P0：升级特征快照模型

现有 `MarketFeature.values` 通用字典可以保留作为兼容层，但不应成为 Phase 3 的唯一正式接口。

正式特征快照至少应包含：

- `feature_snapshot_id`；
- `schema_version`；
- `strategy_version`；
- `calculation_timestamp`；
- `decision_timestamp`；
- `exchange` 或跨交易所范围；
- `symbol`；
- `window_seconds`；
- `book_generation`；
- `source_sequence_id`；
- `source_event_count`；
- `oldest_source_timestamp`；
- `newest_source_timestamp`；
- `data_age_ms`；
- `is_warm`；
- `is_healthy`；
- `unavailable_reasons`；
- 明确类型的特征字段。

不能用 `0` 代表“数据缺失”。缺失、未预热、失序和真正的零值必须明确区分。

#### P0：建立确定性的时钟和调度边界

实时特征计划每秒更新，信号计划每 5 秒检查。需要建立统一调度模型：

- 实时模式使用 UTC 单调推进的决策边界；
- 回放模式使用事件时间驱动的虚拟时钟；
- 相同事件、配置、代码版本和决策边界必须产生相同结果；
- 不得依赖任务调度“碰巧”先后执行；
- 不得使用未来到达的事件补写过去的特征快照。

建议引入：

```text
Clock Protocol
├─ LiveClock
└─ ReplayClock

DecisionScheduler
├─ feature ticks: 1s
└─ signal ticks: 5s
```

#### P0：建立原始数据读取与重放入口

当前已经可靠保存原始载荷，但 Phase 3 需要能够从 Parquet 中重新读取，并通过与实时相同的规范化和处理管线重放。

必须实现：

- 按时间范围、交易所、交易对和频道扫描；
- 按事件时间和本地接收时间排序；
- 同时间戳稳定排序规则；
- 保留 `connection_generation` 和订单簿生命周期；
- 从原始载荷重新执行规范化；
- 与实时模式使用相同的事件分发和特征引擎；
- 支持极速回放和按原始节奏回放。

#### P0：特征健康门控

当以下情况发生时，不得生成“可交易”的特征快照：

- 核心频道为 `STALE`、`RESYNCING` 或 `DISCONNECTED`；
- 订单簿未同步；
- 订单簿 generation 刚切换且窗口尚未重新预热；
- 跨交易所时间对齐超出阈值；
- OI 数据过期；
- 特征队列积压造成数据时效性失真；
- 关键窗口内存在无法解释的数据缺口。

可以生成带不可用原因的观察快照，但不能伪装成正常特征。

### 2.3 建议同步补强的工程项

#### P1：Parquet 小文件压缩

30 分钟验收中约 233 万行形成 8,362 个文件，长期运行会出现小文件问题。

增加离线工具：

```powershell
cvf compact-raw --input data/raw/source --output data/raw_compacted
```

目标：

- 保留现有分区和数据血缘；
- 每文件约 50,000–250,000 行，或约 64–256 MB；
- 输入目录只读；
- 输出到新目录；
- 压缩前后逐行数量、UUID、哈希和分区审计一致。

#### P1：GitHub Actions

仓库应增加持续集成，至少执行：

```text
pytest
ruff check
mypy
pip check
build
wheel clean-install smoke test
```

网络集成测试和长时间采集测试不得放进普通 PR 必过流程，应通过手动工作流或定时任务独立执行。

#### P1：长期数据集

现有 30 分钟数据适合确定性和吞吐回归，不足以验证长期统计特征。

建议建立分层数据集：

| 数据集 | 用途 |
|---|---|
| 30 分钟固定集 | 解析、订单簿、确定性、回归测试 |
| 6 小时集 | 运行稳定性、窗口特征、内存与性能 |
| 24 小时集 | 24 小时分位数、日内变化、Funding 状态 |
| 7 天集 | 不同交易时段和波动环境初步比较 |
| 30 天以上 | 策略研究、时间切分、参数稳定性 |

大型数据集不应直接提交 Git，应维护清单、哈希、schema 和生成说明。

---

## 3. 版本和阶段路线

| 阶段 | 建议版本 | 核心交付 | 是否可发送实时信号 |
|---|---:|---|---|
| Phase 2.5 | 0.2.1 | 事件总线、回放读取、CI、小文件工具 | 否 |
| Phase 3 | 0.3.0 | 实时与回放统一的特征引擎 | 否 |
| Phase 4A–C | 0.4.0 | 评分、信号状态机、信号审计 | 仅本地观察 |
| Phase 4D | 0.4.1 | Telegram/企业微信等通知 | **是，研究型信号** |
| Phase 5 | 0.5.0 | 模拟交易、风控、费用和滑点 | 是，可进行模拟验证 |
| Phase 6 | 0.6.0 | 历史回放、回测和参数敏感性 | 是，可评估小额实盘条件 |
| Phase 7 | 0.7.0 | 监控面板和运行运维 | 是 |
| Phase 8 | 1.0.0 前置阶段 | 可选小额实盘执行 | 需单独批准和验收 |

### 实时发送交易信号的明确节点

最早在 **Phase 4D / v0.4.1** 完成后，系统可以向用户实时发送：

- `LONG_ENTRY`；
- `SHORT_ENTRY`；
- `LONG_EXIT`；
- `SHORT_EXIT`；
- `HOLD`；
- `NO_TRADE`；
- `EMERGENCY_EXIT`。

此时的信号只能标记为：

> 研究型实时信号，不自动下单，尚未完成历史盈利验证。

完成 Phase 5 后，信号才具备模拟成交和风险验证；完成 Phase 6 后，才可以判断是否满足小额人工实盘测试条件。

---

## 4. Phase 2.5：工程衔接与稳定化

> 完成状态：已于 2026-07-24 实现。事件总线、确定性时钟/调度、原始数据扫描与
> 回放、独立目录压缩审计、离线 CLI 和 GitHub Actions 质量门均已落地；原 Phase 2
> 安全边界保持不变。

### 4.1 目标

在不改变 Phase 2 数据采集安全边界的前提下，为特征、回放和持续开发建立正式扩展接口。

### 4.2 开发任务

1. 实现 `NormalizedEventBus`；
2. 支持多个异步消费者；
3. 为每个消费者配置有界队列和背压策略；
4. 增加处理延迟与队列深度指标；
5. 实现 `LiveClock` 和 `ReplayClock` 协议；
6. 实现统一 `DecisionScheduler`；
7. 实现原始 Parquet 扫描器；
8. 实现原始事件稳定排序规则；
9. 实现离线回放命令骨架；
10. 实现 Parquet 小文件压缩与审计工具；
11. 增加 GitHub Actions 质量门；
12. 保证 `cvf` 和 `cvf --once` 继续完全离线。

### 4.3 建议目录

```text
src/cvf/
  pipeline/
    event_bus.py
    consumer.py
    metrics.py
  clock/
    base.py
    live.py
    replay.py
    scheduler.py
  replay/
    raw_reader.py
    ordering.py
    runner.py
  storage/
    compact.py
scripts/
  validate_compaction.py
.github/workflows/
  quality.yml
```

### 4.4 验收标准

- 同一事件可稳定分发给两个以上消费者；
- 消费者失败可观测且不会静默丢失；
- 回放可重新产出与实时规范化等价的事件；
- 事件顺序测试具有确定性；
- 小文件压缩前后数据审计一致；
- GitHub Actions 在 PR 上自动执行质量门；
- Phase 2 的 76 项及后续新增测试全部通过（当前 99 项）；
- wheel 独立安装后命令保持兼容。

---

## 5. Phase 3：实时特征引擎

### 5.1 目标

把实时规范化行情转换成无前视、可回放、可追溯的单交易所与跨交易所特征快照。

### 5.2 Phase 3A：状态存储和窗口基础设施

> 完成状态：已于 2026-07-24 实现。实时与回放共享同一个有界状态消费者；窗口、
> 迟到事件、订单簿预快照缓冲、generation 失效、健康门控和 typed feature schema v1
> 均有确定性测试。当前仍不计算策略特征、不生成信号。

实现每个 `exchange + symbol` 独立状态：

- 成交环形缓冲；
- 价格序列；
- 订单簿前后状态；
- OI 序列；
- 资金费率序列；
- 标记价和指数价；
- 强平事件序列；
- 数据健康和订单簿 generation。

要求：

- 内存有严格上限；
- 按事件时间清理窗口；
- 不使用全量 pandas DataFrame 作为无限增长的实时状态；
- 所有窗口定义包含开闭边界规则；
- 迟到事件处理规则配置化并有测试。

### 5.3 Phase 3B：单交易所特征

> 完成状态：已于 2026-07-24 实现。`SingleVenueFeatureEngine` 在明确的决策时间边界上
> 生成 5 秒、15 秒和 60 秒 typed snapshots，覆盖成交流、连续订单簿、价格与波动、
> OI、拥挤度和公开强平样本；确定性 ID、无前视边界、缺失/未预热原因及联合状态均有
> 独立参考测试。当前仍不计算跨交易所特征、策略评分或交易信号。

#### 成交流

- 5 秒、15 秒、60 秒主动买入额；
- 5 秒、15 秒、60 秒主动卖出额；
- Taker Imbalance；
- 成交额冲量；
- 成交笔数冲量；
- 平均成交大小；
- 大额主动成交占比。

#### 订单簿

- 前五档加权买方深度；
- 前五档加权卖方深度；
- Depth Imbalance；
- 最优价差；
- 相对价差；
- Mid Price；
- Microprice；
- 五档深度行走滑点；
- 买卖盘新增和撤销；
- 简化 OFI；
- OFI Z-score；
- 订单簿短期恢复速度。

OFI 只能基于同步且连续的本地订单簿计算。generation 变化后必须重新预热。

#### 价格与波动

- 5 秒、15 秒、60 秒收益率；
- 价格冲量；
- 实现波动率；
- 1 分钟 ATR；
- 3 分钟前高和前低；
- 最近移动相对 ATR；
- 跳跃和异常波动标记。

#### 持仓量

- 15 秒与 60 秒 OI 变化；
- OI 百分比变化；
- 本交易所内部 OI Z-score；
- 价格与 OI 组合状态；
- OI 数据年龄。

不得直接比较 Binance 与 OKX 的绝对 OI 数值。

#### 拥挤度与基差

- Funding Rate；
- Funding 本交易所内部 Z-score；
- 标记价相对指数价溢价；
- 溢价 Z-score；
- Taker 偏差；
- 价格、OI 与 Funding 联合状态。

#### 强平活动

- 5 秒、15 秒、60 秒公开强平样本名义额；
- 多头和空头强平方向；
- 强平活动 Z-score；
- 强平增加与 OI 下降组合状态。

公开强平频道不代表完整市场总量，字段和文档必须明确使用“公开强平样本活动”。

### 5.4 Phase 3C：跨交易所特征

> 完成状态：已于 2026-07-27 实现。`CrossVenueFeatureEngine` 在显式决策边界选择
> Binance/OKX typed snapshots，输出可配置的时间对齐、价差、方向、订单流、OI 上下文、
> 拥挤度、公开强平样本确认/分歧和完整版本血缘。31 项专项测试覆盖未来数据、阈值边界、
> 预热/健康、确定性、实时/回放一致性及手算公式。lead/lag 仍明确为研究不可用字段，
> 不使用本机到达顺序；不生成评分、信号或订单。

实现：

- Binance 与 OKX 中间价差；
- 价差百分比；
- 价差 Z-score；
- 价格方向一致性；
- Taker Flow 一致性；
- OFI 一致性；
- 价格冲量一致性；
- OI 上下文冲突；
- 跨交易所确认分；
- 分歧惩罚输入；
- lead/lag 研究字段；
- 时间对齐质量；
- 两个平台数据年龄差。

lead/lag 只能作为研究特征。不能仅根据哪个数据包先到达本机判断交易所领先。

### 5.5 Phase 3D：特征持久化与一致性

> 完成状态：已于 2026-07-27 实现。单所与跨所 typed snapshots 统一写入
> `feature_schema=v1/date/symbol/scope`，具备配置化有界队列/批次/刷新/去重、原子发布、
> 完整 source/code/config lineage、严格 reader/filter/audit 和 live/replay 逻辑树精确比较。
> 34 项专项测试覆盖 round trip、篡改、重复 ID、分区、排序、过滤、批次差异、独立引擎
> 一致性、CLI 与无信号/订单边界。

特征保存到：

```text
data/processed/
  feature_schema=v1/
    date=YYYY-MM-DD/
      symbol=BTC-USDT-PERP/
        scope=BINANCE|OKX|CROSS_VENUE/
```

要求：

- 特征 schema 显式版本化；
- 保存配置哈希和代码版本；
- 保存原始事件引用范围；
- 实时和回放生成相同快照；
- 重复回放结果完全一致；
- 浮点容差规则固定；
- 不把特征文件与原始文件混在同一目录。

### 5.6 建议目录

```text
src/cvf/features/
  base.py
  models.py
  availability.py
  rolling.py
  state.py
  trade_flow.py
  orderbook.py
  price.py
  open_interest.py
  crowding.py
  liquidation.py
  cross_venue.py
  pipeline.py
src/cvf/storage/
  features.py
```

### 5.7 Phase 3 验收标准

1. 实时事件可以每秒生成特征快照；
2. 回放使用同一套特征代码；
3. 不存在前视偏差；
4. 窗口和预热规则明确；
5. 数据异常时输出不可用原因；
6. 订单簿 generation 变化会使相关特征重新预热；
7. 每个特征有独立参考计算测试；
8. 30 分钟固定集重复执行结果一致；
9. 特征吞吐高于实时输入速度；
10. 6 小时运行无持续内存增长；
11. 特征文件可读并通过 schema 审计；
12. 不输出任何做多或做空信号；
13. pytest、Ruff、严格 mypy、build、wheel 验收全部通过。

---

## 6. Phase 4：评分、信号与实时通知

### 6.1 Phase 4A：交易所评分

按照配置计算：

```text
exchange_score =
    0.30 × taker_imbalance_z
  + 0.25 × ofi_z
  + 0.20 × price_impulse_z
  + 0.15 × oi_impulse_z
  + 0.10 × liquidation_impulse_z
  - liquidity_penalty
  - crowding_penalty
  - data_health_penalty
```

权重是研究假设，必须配置化、版本化并记录在信号中。

### 6.2 Phase 4B：综合评分

```text
combined_score =
    0.45 × binance_score
  + 0.45 × okx_score
  + 0.10 × cross_exchange_confirmation
  - divergence_penalty
```

Phase 4 初期只实现规则基线，不引入机器学习。

### 6.3 Phase 4C：信号状态机

输出：

- `LONG_ENTRY`；
- `SHORT_ENTRY`；
- `LONG_EXIT`；
- `SHORT_EXIT`；
- `HOLD`；
- `NO_TRADE`；
- `EMERGENCY_EXIT`。

每个信号至少包含：

- `signal_id`；
- `signal_type`；
- `symbol`；
- `decision_timestamp`；
- `generated_timestamp`；
- `expires_at`；
- `feature_snapshot_id`；
- `feature_schema_version`；
- `strategy_version`；
- Binance Score；
- OKX Score；
- Combined Score；
- 置信等级；
- 建议执行平台；
- 参考价格；
- 参考止损和止盈；
- 结构化 `reason_codes`；
- `blocking_conditions`；
- 数据健康摘要；
- 信号是否为重复、替换或失效。

状态机必须处理：

- 同一行情连续触发时去重；
- 冷却时间；
- 信号 TTL；
- 重启后的状态恢复；
- 已存在参考仓位时不重复开仓；
- 数据异常时发 `NO_TRADE` 或 `EMERGENCY_EXIT`；
- 平多和做空开仓不能混淆；
- 平空和做多开仓不能混淆。

### 6.4 Phase 4D：实时通知

建议优先实现 Telegram，并保留统一接口：

```text
NotificationProvider
├─ TelegramProvider
├─ WeComProvider
├─ FeishuProvider
└─ EmailProvider
```

通知模块要求：

- API Token 只通过环境变量或密钥管理传入；
- 不将凭据写入 Git；
- 信号发送去重；
- 失败重试和指数退避；
- 最大重试次数；
- 失败记录和死信；
- 发送结果持久化；
- 同一信号可追踪到通知消息；
- 通知失败不能阻塞市场数据和特征管线；
- 支持测试模式和本地控制台模式；
- 默认不开启外部通知。

示例通知：

```text
CVF-01 研究型实时信号

交易对：BTC-USDT-PERP
信号：LONG_ENTRY
决策时间：2026-07-24 12:35:10 UTC+8
建议观察平台：BINANCE

Binance Score：2.08
OKX Score：1.91
Combined Score：2.03

触发原因：
- 两个平台主动买入增强
- OFI 同方向
- 价格冲量确认
- OI 随价格增加
- 跨平台价差正常

参考价格：--
参考止损：--
参考止盈 1：--
参考止盈 2：--
信号有效期：5 秒

研究信号，不自动下单。
```

### 6.5 Phase 4 验收标准

1. 正确输出全部七类信号；
2. 信号原因和阻断原因结构化；
3. 信号去重、TTL 和冷却生效；
4. 数据不健康时不会发入场信号；
5. 重启不会重复发送旧信号；
6. 通知失败不会影响行情处理；
7. 每个信号的未来 5、15、30 分钟结果可追踪；
8. 本地观察模式与通知模式使用相同信号源；
9. Phase 4D 完成后可以实时收到研究型信号；
10. 不连接交易账户、不发送订单。

---

## 7. Phase 5：模拟交易和风险控制

### 7.1 模拟执行

实现：

- 基于前五档订单簿的市价深度行走；
- 部分成交；
- 平均成交价；
- 手续费；
- 滑点；
- Funding；
- 延迟惩罚；
- 流动性不足拒单；
- 下单与成交时间差。

禁止按中间价无滑点成交。

### 7.2 风险控制

- 初始模拟余额 10,000 USDT；
- 单笔风险 0.2%；
- 单日亏损上限 1%；
- 单日最多 10 笔；
- 连续亏损 3 笔暂停；
- 同时最多一个仓位；
- 最大实际名义杠杆 2 倍；
- 禁止亏损加仓；
- 禁止马丁格尔；
- 禁止扩大止损。

### 7.3 出场逻辑

- 初始止损：`0.55 × 1 分钟 ATR`；
- TP1：`0.8 ATR`，平 50%；
- TP2：`1.3 ATR`；
- TP1 后移动剩余止损；
- 10 分钟时间止损；
- 30 分钟硬退出；
- 综合评分明显反转退出；
- 数据或流动性严重异常时紧急退出。

### 7.4 验收标准

- 所有信号可转换为模拟订单或明确拒绝原因；
- 订单簿深度不足不会伪造成交；
- 费用、滑点、Funding 和 PnL 可审计；
- 风险限制有单元测试；
- 同一历史输入产生相同模拟交易；
- 实时信号和模拟成交完整关联；
- 此阶段后，实时信号才具备初步人工参考价值。

---

## 8. Phase 6：历史回放、回测和研究验证

### 8.1 回放要求

- 读取原始 Parquet；
- 复用规范化、订单簿、特征、信号和模拟交易代码；
- 支持原速、倍速和极速；
- 支持暂停和范围选择；
- 使用虚拟时钟；
- 保证确定性；
- 保留数据缺口和重连语义。

### 8.2 回测指标

- 总交易次数；
- 多单和空单次数；
- 胜率；
- 平均盈利和平均亏损；
- 盈亏比；
- Profit Factor；
- 总收益；
- 最大回撤；
- Sharpe；
- Sortino；
- 手续费；
- 滑点；
- Funding；
- 平均持仓时间；
- 信号到成交延迟；
- 不同交易对、交易所和波动状态表现；
- 参数敏感性；
- 信号覆盖率和拒绝原因分布。

### 8.3 研究纪律

- 时间顺序训练、验证和测试；
- 重叠标签设置 embargo；
- 不随机打乱时间序列；
- 不在测试集调参数；
- 参数轻微变化后仍需稳定；
- 费用和滑点翻倍压力测试；
- 数据异常时段单独报告；
- 多空方向分别评估；
- BTC 和 ETH 分别评估；
- 不以单一胜率判断策略有效性。

### 8.4 进入小额实盘测试的最低条件

以下全部满足后，才可以提出小额实盘申请：

1. 独立测试区间扣除成本后为正；
2. Profit Factor、回撤和收益分布可接受；
3. 参数附近存在稳定区域；
4. 不同月份和不同波动状态不完全失效；
5. 实时模拟结果与历史预期基本一致；
6. 连续运行和通知可靠；
7. 极端行情风险有明确上限；
8. 所有数据缺口和异常可审计；
9. 已完成书面风险评审；
10. 用户明确批准进入下一阶段。

---

## 9. Phase 7：监控和运维

建立轻量监控面板，展示：

- 两家交易所连接状态；
- 每个频道健康；
- 本地订单簿状态和 generation；
- 当前价格和价差；
- 特征是否预热；
- Binance Score；
- OKX Score；
- Combined Score；
- 当前信号；
- 最近信号原因；
- 通知发送状态；
- 当前模拟仓位；
- 当日 PnL；
- 风险锁定状态；
- 队列深度；
- Parquet 写入和特征处理延迟；
- 最近异常、重连和重同步。

监控页面不得拥有真实交易权限。

---

## 10. Phase 8：可选真实交易阶段

该阶段不属于当前 MVP，必须单独立项。

进入前需要：

- 私有 API 安全设计；
- 最小权限 API Key；
- 禁止提现权限；
- 密钥管理；
- 下单幂等；
- 状态对账；
- 只减仓保护；
- 交易所故障应急；
- 网络不确定性处理；
- 仓位和余额一致性；
- 人工急停；
- 小额限额；
- 完整审计日志；
- 独立安全评审。

默认仍应优先采用“实时信号 + 人工确认”，而不是直接自动交易。

---

## 11. 推荐 Git 工作流

每个阶段使用独立分支：

```text
feature/phase-2-5-pipeline
feature/phase-3-feature-engine
feature/phase-4-signal-engine
feature/phase-4-notifications
feature/phase-5-paper-trading
feature/phase-6-replay-backtest
```

开发规则：

1. 每个阶段先建立 Issue/任务清单；
2. 小批次提交；
3. 所有变更通过 PR；
4. PR 必须通过自动质量门；
5. 禁止直接把未验证策略参数合并为“有效参数”；
6. 每个阶段发布版本标签；
7. 每个版本保留配置哈希、测试结果和验收记录；
8. 行情协议变更必须增加 fixture 和回归测试。

---

## 12. 下一阶段首批任务清单

Phase 3 不应直接从公式实现开始，建议按照以下顺序：

### 批次 1：事件管线

- [x] 新增 `NormalizedEventBus`；
- [x] 支持多消费者；
- [x] 有界队列与背压；
- [x] 顺序和关闭测试；
- [x] 将当前计数逻辑改为一个消费者。

### 批次 2：时钟与调度

- [x] `Clock` 协议；
- [x] `LiveClock`；
- [x] `ReplayClock`；
- [x] 1 秒特征 tick；
- [x] 5 秒信号 tick 预留；
- [x] 边界和确定性测试。

### 批次 3：回放读取

- [x] 原始 Parquet reader；
- [x] 分区过滤；
- [x] 稳定排序；
- [x] 重新规范化；
- [x] 与实时事件等价测试。

### 批次 4：特征模型和状态

- [x] 特征 schema v1；
- [x] 可用性和缺失原因；
- [x] 每交易所、交易对状态；
- [x] 有界滚动窗口；
- [x] generation 变化处理。

### 批次 5：基础特征

- [x] Taker Imbalance；
- [x] Spread、Mid、Microprice；
- [x] Depth Imbalance；
- [x] Returns 和 Realized Volatility；
- [x] OI Change；
- [x] 单元参考计算。

### 批次 6：高级特征

- [x] OFI；
- [x] ATR 与突破；
- [x] Funding 与基差；
- [x] 强平活动；
- [x] Z-score 和预热规则。

### 批次 7：跨交易所特征

- [x] 时间对齐；
- [x] 价差；
- [x] 方向一致性；
- [x] 冲突和确认；
- [x] 对齐质量。

### 批次 8：持久化与验收

- [x] Feature Parquet writer；
- [x] 实时与回放一致性；
- [ ] 30 分钟固定集回归；
- [ ] 6 小时性能验证；
- [ ] 文档和 wheel 验收。

---

## 13. 当前最重要的里程碑

### 里程碑 A：v0.3.0

系统能够可靠地把实时市场数据转换为可回放的特征快照。

### 里程碑 B：v0.4.0

系统能够生成完整的做多、做空、平仓和不交易状态，但只在本地记录。

### 里程碑 C：v0.4.1

系统能够通过 Telegram、企业微信或其他通知渠道实时发送研究型交易信号。

### 里程碑 D：v0.5.0

系统能够对实时信号进行模拟成交、风险控制和盈亏记录。

### 里程碑 E：v0.6.0

系统能够完成历史回放、回测、成本压力测试和参数敏感性分析。

---

## 14. 结论

当前项目已经完成最困难的基础之一：可信、可恢复、可追溯的双交易所公共行情采集。

下一步不应立即编写买卖阈值，而应先完成：

```text
事件分发
→ 确定性时钟
→ 回放读取
→ 特征状态
→ 单交易所特征
→ 跨交易所特征
```

在 Phase 4D / v0.4.1 完成后，项目可以开始实时向用户发送研究型做多、做空和平仓提示。

在 Phase 5 完成前，这些提示不应被视为已经验证的交易建议；在 Phase 6 完成前，不应据此进入正式合约实盘。
